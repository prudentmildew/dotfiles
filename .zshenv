# Read by every zsh (scripts too), before /etc/zshrc. Keep it to variables
# that must be set that early.

# Stop macOS Terminal from saving/restoring per-session history.
export SHELL_SESSIONS_DISABLE=1
